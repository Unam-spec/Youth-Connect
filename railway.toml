[build]
builder = "nixpacks"
buildCommand = "pnpm run build --filter=@workspace/api-server"

[deploy]
startCommand = "pnpm run start --filter=@workspace/api-server"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
