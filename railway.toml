[build]
builder = "nixpacks"
buildCommand = "npm install --legacy-peer-deps --ignore-scripts && npm run build --workspace=artifacts/api-server"

[deploy]
startCommand = "npm run start --workspace=artifacts/api-server"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
